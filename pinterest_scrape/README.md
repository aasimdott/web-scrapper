## 📋 Prerequisites

Before running the application, ensure you have the following system dependencies installed:

1. **playwright**
    - Install via pip `pip install playwright`
2. **BeautifullSoup**
    - Install via pip `pip install bs4`
  
## Usage

1. First run the pinterest_p.py to manuall login 
2. Run pinterest_miner.py to extract data
